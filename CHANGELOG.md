# ChunkPatch 變更紀錄

本文件依 Git 提交歷史整理，記錄 Minecraft 1.19.4 分支的功能變更。版本依發布順序排列；目前可追溯的第一個版本為 `0.1.1`，儲存庫中沒有 `0.1.0` 的提交紀錄。

## 0.1.1 - 2026-08-15

最初可追溯版本。

### 新增

- 加入 Fabric 1.19.4 單人世界區塊補圖工具。
- 按 `K` 開啟預覽介面，掃描目前維度的 Anvil `region/*.mca` 位置表。
- 以綠色、黑色及紅色區分已儲存、未生成與位置表異常的區塊。
- 自動偵測存檔最外圍邊界，並支援輸入方塊座標或拖曳黃框調整生成範圍。
- 估算既有、異常與待生成區塊數量，只生成範圍內真正缺少的區塊。
- 支援每 Tick 生成 1～8 個區塊，預設為 1。
- 加入磁碟空間保護：可用空間低於 2 GiB 時阻止開始或自動暫停。
- 分批儲存世界與生成進度，關閉並重新啟動遊戲後可繼續。
- 加入 `/chunkfill scan`、`status`、`pause`、`resume`、`stop` 與 `start` 備援指令。

### 安全性

- 不覆寫已存在的完整區塊，也不自動處理位置表異常的區塊。
- 單次選取面積限制為最多 5,000,000 個區塊。

## 0.1.2 - 2026-08-16

### 新增

- 將預覽改為全螢幕介面。
- 支援滑鼠滾輪縮放、拖曳平移，以及顯示游標所在的方塊與區塊座標。
- 生成期間即時更新預覽，並以青色標示目前進度。

### 改善

- 重新配置可縮放視窗中的介面與邊界操作，讓預覽在不同視窗尺寸下維持可用。

## 0.1.3 - 2026-08-16

### 新增

- 掃描 Chunk NBT 的 `Status`，不再只判斷區塊是否占有 `.mca` 位置表項目。
- 以綠色顯示 `full`、橘色顯示未完成 ProtoChunk、黑色顯示缺少區塊、紅色顯示異常資料。
- 統計完整、未完成、異常與待處理的區塊數量。
- 讓 Minecraft 接續生成合法的半成品區塊至 `full`。

### 安全性

- 保留既有 `full` 區塊及異常資料，不進行覆寫。

## 0.1.4 - 2026-08-16

### 新增

- 加入持久化區域掃描快取，儲存於 `config/chunkpatch-scan-cache`。
- 首次完整掃描後，只重新掃描檔案大小或修改時間有變動的 `.mca`，縮短後續掃描時間。

### 改善

- 調整掃描結果資料結構與測試，以支援快取載入及增量更新。

## 0.1.5 - 2026-08-16

### 變更

- 將「可用」門檻從 `full` 改為 Xaero World Map 1.39.12 可繪製的 `features` 或更後期狀態。
- 綠色代表 `features`、`light`、`spawn` 或 `full`；橘色代表尚未到達 `features` 的 ProtoChunk。
- 生成只補到 Xaero 可繪製的 `FEATURES` 階段，玩家日後走近時再由原版完成至 `full`，降低預生成負擔。
- 已達 `features` 的區塊不會重做。

### 新增

- 掃描與生成期間依照實際速度顯示預估剩餘時間及大約完成時刻。
- 生成 ETA 排除暫停時間，讓恢復後的估算更準確。

## 0.1.6 - 2026-08-16

### 效能改善

- 將掃描移至低優先序的專用單執行緒，降低大型世界掃描對遊戲的干擾。
- 在同一個區域檔內重用 zlib 解壓器及讀取緩衝區，減少原生資源配置與垃圾回收壓力。
- 掃描大量區域檔時定期寫入安全檢查點，避免中斷後必須從頭重掃。

## 0.1.7 - 2026-08-16

### 新增

- 讀取 Xaero World Map 的 `.xwmc` 地圖快取，讓已存在地表圖磚的區域直接判定為綠色。
- 支援 Xaero 原生的 4×4 區塊葉圖磚覆蓋判定。
- 加入 Xaero 快取解析與座標覆蓋的自動化測試。

### 變更

- 綠色現在代表世界區塊已達 `features`，或 Xaero 已有可顯示的地表圖磚；橘色則代表兩者皆未達標。

## 0.1.8 - 2026-08-16

### 修正

- 修正 Xaero 已有圖磚、但世界掃描結果中沒有對應狀態時，預覽仍顯示為黑色或未納入範圍的問題。
- 修正縮略預覽中少量橘色區塊覆蓋大量綠色區塊的著色問題；混合狀態改以數量較多的一方顯示。
- 修正縮放密度較高時，少量已繪製區塊因取樣四捨五入而消失的問題。

### 測試

- 擴充 Xaero 覆蓋掃描測試，包含缺少世界狀態與混合預覽狀態的情境。

## 0.1.9 - 2026-08-17

### 修正

- 修正補完後 Xaero 地圖仍出現遺漏區塊的問題：移除 0.1.7 引入的 `.xwmc` 快取覆蓋判定。Xaero 的 4×4 葉圖磚只要其中任一區塊被畫過就會建立，無法證明整葉 16 個區塊都已繪製，先前據此跳過生成會留下永久破圖；現在只以世界區塊 `Status` 為準。已被 Xaero 畫過的區塊必然已達 `features`，不會被重做。
- 修正存檔狀態為 `heightmaps` 的區塊被誤判為不可繪半成品的問題（1.19.4 的狀態鏈為 `features → light → spawn → heightmaps → full`）。
- 修正放大檢視時預覽仍以 180×96 統計格子著色、不同狀態的區塊顏色混在一起的問題。
- 當每個區塊在畫面上佔滿至少 1 像素且可見範圍不超過 131,072 個區塊時，改為逐區塊繪製真實狀態：綠色＝可繪、橘色＝不可繪半成品、紅色＝異常、黑色＝缺少。
- 縮小檢視時維持原本的統計密度著色。

### 變更

- 區域掃描快取版本升至 3；因 `heightmaps` 重新分類，舊快取會自動作廢並於下次掃描重建。

## 0.1.10 - 2026-08-18

### 新增

- 掃描與生成進度訊息加上「尚餘」的實際數量（區域檔數 / 區塊數），不再只顯示百分比與預估時間。
- 掃描時優先處理檔案較小的 `.mca`，讓進度與快取檢查點更快開始累積，而非卡在少數大檔案後面。

### 修正

- 修正控制面板上方的狀態文字（進度＋預估剩餘時間）過長時被裁切看不到的問題，現在會依「｜」分隔自動換行顯示。

## 0.1.11 - 2026-08-18

### 新增

- 生成迴圈加入落後自動煞車：偵測到上一個 tick 花費超過 200ms（正常 tick 預算 50ms 的 4 倍）時，本次 tick 跳過生成，讓伺服器有機會追上進度，避免長時間持續落後對 GC 造成過大壓力。此為前次遊戲 JVM 原生崩潰（GC 執行緒 EXCEPTION_ACCESS_VIOLATION）調查後的修正。

## 0.1.12 - 2026-08-18

### 新增

- 生成迴圈加入單次區塊冷卻機制：單次 `getChunk` 呼叫本身無法被中斷，若遇到大片從未生成過的地形，一次呼叫就可能連鎖生成整片鄰居區塊、耗時數秒，光靠 tick 間隔煞車無法防範。現在偵測到單次呼叫超過 150ms，會立即停止本次 tick 並強制冷卻 40 個 tick 再繼續，避免連續多次巨量尖峰疊加。此為換用 Temurin 17.0.20 後仍然發生相同 JVM 原生崩潰（GC 執行緒 EXCEPTION_ACCESS_VIOLATION）調查後的修正；由於兩個不同版本的 JVM 都出現相同崩潰特徵，根因可能不是 JVM 版本本身，而是持續高強度記憶體配置壓力下的環境穩定性問題。

## 0.2.0 - 2026-08-18

生成執行模型的重構版本，依據 `ChunkPatch_REFACTOR_PLAN.md` 的調查與設計實作。0.1.11／0.1.12 的落後煞車與單次冷卻只能在同步 `getChunk()` 造成的巨量配置尖峰**發生之後**才反應；這個版本改成在尖峰**發生之前**就節流，從架構上移除同步等待。

### 變更（重大）

- **移除伺服器 tick 內同步呼叫 `getChunk(FEATURES, true)`。** 改為透過專用協調執行緒呼叫 Minecraft 的 `getChunkFuture()`（非主執行緒分支才會真正不阻塞；直接在主執行緒呼叫或用 `supplyAsync` 包住同步 `getChunk()`，最終都還是會落回 `managedBlock()` 阻塞，兩者都試過並確認無效）。生成完成的回呼一律透過 `server.execute(...)` 送回伺服器執行緒才會修改任何狀態。
- **同一時間最多只有一個區塊在生成（single in-flight）。** 前一個目標的完成回呼處理完之前，絕不提交下一個目標；這個限制寫死在架構裡，不開放給使用者調整並發數。
- 移除 `chunksPerTick` 與「每 Tick 區塊數」欄位／指令參數；改為在**提交下一個目標前**檢查伺服器平均 tick 時間與記憶體使用率（含遲滯區間），負載過高時暫緩提交，而不是尖峰發生後才冷卻。連帶移除 0.1.11／0.1.12 的 `LAG_BACKOFF_THRESHOLD_NANOS`、`SLOW_CHUNK_NANOS`、`SLOW_CHUNK_COOLDOWN_TICKS` 等事後煞車機制。
- 暫停／停止改為「排空」語意：若當下有區塊正在生成，會先進入 `PAUSING`／`STOPPING` 狀態、停止提交新目標，等目前這個完成後才真正暫停或停止，而不是強制取消 Minecraft 已經建立的生成 future。
- 加入世代 session 標記：停止、切換維度或伺服器關閉時會讓進行中的 session 失效，之後才抵達的過期完成回呼會被安全忽略，不會誤改到新一輪的狀態。

### 修正

- 修正預覽資料的跨執行緒競爭：`PreviewData` record 過去把可變陣列直接暴露給用戶端渲染執行緒讀取，同時伺服器執行緒又在原地修改同一份陣列。現在拆成伺服器持有的可變計數與發布用的不可變 `PreviewSnapshot`，讀者拿到的一定是完整、不會事後被改動的快照。
- 修正掃描期間若 Minecraft 剛好對同一個 `.mca` 自動存檔，可能讀到新舊資料混合的「撕裂讀取」問題：現在每個區域檔在掃描前後都會比對檔案大小與修改時間戳記，不一致就重試（最多 3 次）；持續不穩定的區域檔不會被誤判為異常，也不會寫入快取，下次掃描會再重試。
- 移除生成路徑上的 `catch (Throwable)`：不再嘗試把 `VirtualMachineError` 這類 JVM 層級的嚴重狀態當成一般可恢復的生成失敗處理。

### 測試

- 新增 `ChunkGenerationSchedulerTest`：驗證同一時間只會有一個生成請求在途、完成後才會提交下一個、future 失敗會正確清空且不誤重試、session 失效後過期回呼不會造成任何副作用、半成品區塊完成後會被正確升級、異常／已完成區塊不會被重新提交。
- 新增 `PreviewSnapshotTest`：驗證已發布的快照不會被之後的伺服器端修改影響。
- 新增 `RegionScannerConcurrentMutationTest`：以背景執行緒持續觸碰區域檔模擬 Minecraft 同時寫入，驗證掃描過程不會把撕裂讀取誤判為異常。

### 已知限制

- 這次重構大幅降低了 ChunkPatch 造成的 GC／記憶體配置壓力，但**不能證明**先前的 JVM 原生崩潰（`GCTaskThread` 內 `EXCEPTION_ACCESS_VIOLATION`）100% 解決——如果乾淨環境下仍然重現，根因很可能在 JVM/硬體/驅動層級而不是本模組。建議依 `ChunkPatch_REFACTOR_PLAN.md` 附錄 C 的診斷順序繼續排查。

## 0.2.1 - 2026-08-18

依據 `ChunkPatch_0.2.0_POST_REFACTOR_REVIEW.md` 的複審結果，補齊 0.2.0 遺留的生命週期安全問題。複審結論是 0.2.0 的單一在途非同步架構方向正確、不需要再次重寫，剩下的都是「非同步回呼在世界關閉/切換時可能作用到過期狀態」這類生命週期問題。

### 修正（P0／P1，必要）

- **伺服器關閉時只讓 session 失效，沒有真正釋放引用。** `onServerStopping()` 原本只呼叫 `scheduler.invalidateSession()`，但 `scan`、`selected`、`dimensionId`、`activeServer` 等欄位仍保留舊世界的物件圖（包含整個 `MinecraftServer`/`ServerLevel`/`ServerChunkCache` 鏈），讓這個 mod 範圍、長期存活的 controller 一直抓著已關閉世界的記憶體不放。現在 `onServerStopping()` 會完整釋放這些世界持有的引用、重置暫時性計數，並讓生成與掃描的 session 都失效；但**不會**清除硬碟上的續傳進度——正常關閉世界不等於使用者主動取消工作，下次掃描仍應該能提議續傳。
- **非同步區域掃描沒有 session 保護。** `requestScan()` 在背景執行緒掃描期間，世界可能已經關閉或使用者已經重新掃描；舊版沒有任何機制阻止一個過期掃描的進度或結果覆蓋新的狀態。現在比照生成排程器加入 `scanSessionId`，`updateScanProgress`／`completeScan` 都會先比對送出時的 session 與目前 session、以及送出時的 `MinecraftServer` 是否仍是目前使用中的伺服器，任一不符就直接捨棄。
- **`marshalToOwnerThread` 在沒有現存伺服器時會就地執行任務。** 原本的備援邏輯是「找不到 `activeServer` 就直接在呼叫端執行緒跑」，但呼叫端可能是生成協調執行緒或 Minecraft 世界生成完成執行緒——這會讓 controller/scan 狀態被伺服器執行緒以外的執行緒修改，違反整個類別「狀態只能被擁有者執行緒修改」的前提。現在找不到 `activeServer` 時直接捨棄該回呼。

### 修正（P2，建議）

- 區域檔穩定讀取比對從 `FileTime.toMillis()`（截斷到毫秒）改成直接比較完整 `FileTime`，避免部分檔案系統的次毫秒級寫入時間差被誤判為「沒有變化」。
- README 補上實務上建議的 `-Xmx` 設定：記憶體閘門看的是「已用／上限」比例，把 `-Xmx` 開得遠超過實際需求（例如 25～32 GiB）會讓同一波配置尖峰在比例上更難被偵測到；一般驗證建議 `-Xmx8G`，較大型整合包約 `-Xmx10G`，依實測調整。

### 未採用

- 複審附錄提到「完成與下一次提交之間至少間隔一個 tick」是可選的安全邊際，且明確建議不要在沒有實測證據前就加上去——目前保持原樣，等實測顯示背對背提交確實過於激進再考慮。

### 測試

- 新增 `ChunkFillControllerScanLifecycleTest`：過期 session 的掃描進度／結果不會套用、伺服器已不是目前使用中的伺服器時結果會被捨棄、關閉伺服器會讓進行中的掃描 session 失效、世界 A 的過期掃描不會影響世界 B 的狀態。
- 新增 `ChunkFillControllerShutdownLifecycleTest`：暫停／停止會先等待目前在途的目標完成才真正轉換狀態、排空期間若目標失敗仍會到達要求的終止狀態、伺服器關閉後過期完成不會被送到（現已不存在的）擁有者執行緒、關閉後開啟第二個世界不會被舊世界的過期完成影響。這些測試透過 Mockito 模擬 `MinecraftServer`／`ServerLevel`／`ServerChunkCache`，並在測試環境下呼叫 `Bootstrap.bootStrap()` 以初始化 Minecraft 註冊表；新增 `testImplementation "org.mockito:mockito-core"` 依賴，並在 `test` 任務加上 `net.bytebuddy.experimental=true`（目前開發環境的 JDK 版本比 Mockito 內建 Byte Buddy 官方支援的版本新）。
- 修正 `ProgressStore` 的設定檔路徑改為延遲解析（不再是靜態欄位），並讓 `load()`／`save()`／`clear()` 各自完整包住例外處理；這是為了讓上述測試可以在沒有真正 Fabric Loader 執行環境的情況下驅動 `ChunkFillController` 的成功路徑，同時也讓這個類別在正式環境下對設定目錄解析失敗更有韌性（不會整個 class 永久失效）。

## 0.2.2 - 2026-08-18

補上 0.2.1 當時判斷「沒有實測證據前不加」而暫緩的 §9 一次 tick 安全邊際；使用者後續指示直接加上去。

### 新增

- 生成排程加入最小提交間隔：一個目標完成（成功或失敗）之後，下一個目標最早只能在**下一個** tick 提交，絕不會在同一個 tick 內背對背送出。這不是舊版的事後冷卻機制，只是給 Minecraft 一個固定、極小的排程空檔，讓存檔／記憶回收／一般遊戲邏輯在兩次提交之間有機會執行。以 `MinecraftServer.getTickCount()` 實作，`ChunkFillController` 新增 `nextEligibleTick` 欄位，並在 `start()`／`resume()` 重置。

### 測試

- `ChunkFillControllerShutdownLifecycleTest` 新增 `doesNotSubmitTheNextTargetOnTheSameTickAsTheLastCompletion`：驗證同一 tick 內完成一個目標後不會立刻提交下一個，直到下一個 tick 才會提交。

## 0.2.3 - 2026-08-18

依據 `ChunkPatch_0.2.0_FINAL_REVIEW.md` 的最終複審，完成建議在進入長時間 soak test 前要做的最後兩項程式碼強化。複審結論是核心非同步架構已經是正確基準，不需要再變動，剩下的都是關閉時序上的邊界情況。

### 修正

- **`MinecraftChunkGenerationGateway` 加入關閉守衛。** `ExecutorService.shutdown()` 允許已排入佇列的工作繼續執行完畢；如果 `requestFeatures()` 剛好在 `close()` 之前把工作排進協調執行緒的佇列，舊版會讓那個已排隊的工作照樣呼叫已經要被關閉的世界的 `ServerChunkCache.getChunkFuture()`。過期的完成結果本來就會被 `GenerationScheduler` 的 session 機制安全忽略，但工作本身理想上根本不該去碰那個正在拆除的 `ServerChunkCache`。現在加入 `AtomicBoolean closed`，在 API 進入點與佇列中的工作內都各自檢查一次（兩次檢查是必要的，因為 `close()` 可能剛好發生在通過第一次檢查、工作真正執行之間），關閉後的請求會直接以失敗的 future 收場，不會呼叫到底層的 `ServerChunkCache`。
- **`RegionScanCache` 改用完整時間戳記精度。** 掃描時的穩定讀取比對已經改用完整 `FileTime`（見 0.2.1），但磁碟快取的比對鍵仍然是 `toMillis()` 截斷過的毫秒值——如果某些檔案系統回報次毫秒精度、同一個區域檔在同一毫秒內被寫入兩次且大小剛好不變，快取可能誤判成「沒有變化」而回傳過期資料。快取格式版本升到 4，改儲存 `epochSecond` + `nano` 兩個欄位並以此比對，跟穩定讀取的精度一致。舊版（v3 以下）快取會被直接忽略並在下次掃描時重建，不需要遷移。

### 測試

- 新增 `MinecraftChunkGenerationGatewayTest`：關閉後送出的請求會立即以失敗結束、且不會呼叫到底層 `ServerChunkCache`；已經排入佇列但關閉時尚未執行的工作，執行時會觀察到已關閉狀態並同樣不會呼叫底層 `ServerChunkCache`（用 `CountDownLatch` 佔住協調執行緒來確定性地重現這個排隊時序，不依賴猜測的執行緒交錯）。
- 新增 `RegionScanCacheTest`：大小相同但奈秒精度不同的修改時間視為快取未命中；大小與完整時間戳記都相同才視為命中；版本不符的快取檔會被忽略而非部分信任。
